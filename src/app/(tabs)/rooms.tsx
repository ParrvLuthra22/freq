import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function RoomsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Rooms"
      title="Campus rooms,"
      accent="coming later."
      description="Events and campus-scoped spaces for people who share a frequency, not just a location."
      note="Empty-state teaser for now — full build is a later milestone."
    />
  );
}
